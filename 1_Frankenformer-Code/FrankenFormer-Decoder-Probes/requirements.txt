# Core HF stack (CPU/GPU-agnostic)
# pyarrow==14.0.1 # Handled by CC module
# Pin to cluster-provided wheels to avoid pyarrow build attempts
huggingface-hub==0.36.0
# ModernBERT support requires a newer Transformers release
transformers==4.47.0
# Align tokenizers with transformers 4.47.0 expectations
tokenizers==0.21.1
# Use the cluster wheel to avoid pulling a newer datasets that drags in pyarrow
datasets==2.18.0
# datasets>=2.14.0

# Data handling
# Keep NumPy <2 for ABI compatibility with prebuilt extensions on the cluster
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
scikit-learn>=1.3.0
scipy>=1.11.0

# Storage
h5py>=3.10.0,<3.13.0
sqlalchemy>=2.0.0

# Configuration
pyyaml>=6.0

# Progress and utilities
tqdm>=4.65.0
psutil>=5.9.0

packaging>=23.0
 accelerate>=0.26.0
