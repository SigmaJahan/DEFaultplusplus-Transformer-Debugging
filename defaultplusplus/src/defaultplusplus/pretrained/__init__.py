"""Reserved namespace for shipped pretrained diagnostic-model weights.

The ``weights/`` subdirectory will hold model checkpoints distributed
with the package once they are stable. It is intentionally empty in
the current release.
"""
