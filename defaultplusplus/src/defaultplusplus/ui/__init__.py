"""Reserved namespace for the runtime user-interface helpers.

This subpackage will hold CLI / dashboard utilities that consume the
outputs of :class:`defaultplusplus.FeatureExtractor`. It is
intentionally empty in the current release.
"""
